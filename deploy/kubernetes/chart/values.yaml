image: task-agent-instances:local
imagePullPolicy: IfNotPresent
controller:
  maxWorkers: 3
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: "1"
      memory: 512Mi
