/** Shared by model stages and managers that author direct verification commands. */
export const WORKER_ENVIRONMENT_INSTRUCTIONS = `Inspect repository manifests and verify runtime commands before implementation or verification. The standard worker image includes Node/npm, Python/pytest/uv, Java/Maven and Rust/Cargo. Install project dependencies using declared versions/lockfiles.
For a workspace-local Python environment use python -m venv --copies .venv and then .venv/bin/python -m pip install with the project's declared extras. Plain venv creates external interpreter symlinks that the final input snapshot rejects. Keep dependencies and partial work; do not delete the environment to make snapshotting pass. Do not weaken input-closure checks.
Use explicit environment executables such as .venv/bin/python for checks. Prefer direct argv or /bin/sh -c; login shells (/bin/sh -lc) reset the image PATH and can select /usr/bin/python without pytest. A restored workspace retains its files, but each execution must verify its interpreter and dependencies. Read declared CLI entry points and --help; do not invent python -m <package> commands when the package has no __main__.py.`
